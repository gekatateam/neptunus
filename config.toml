[common]
  log_level = "debug"
  log_format = "pretty"
#  http_port = ":9601"
    [common.log_fields]
      runner = "local"
      dc = "east-01"

[engine]
  storage = "fs"
  [engine.fs]
    directory = ".pipelines"
    extention = "toml"
