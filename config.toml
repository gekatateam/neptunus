[common]
  log_level = "info"
  log_format = "json"
  http_port = ":9601"
    [common.log_fields]
      runner = "local"
      dc = "east-01"
      host = "${COMPUTERNAME}"

[engine]
  storage = "fs"
  [engine.fs]
    directory = ".pipelines"
    extention = "toml"
